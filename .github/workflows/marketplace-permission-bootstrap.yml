name: Marketplace validation bootstrap
on:
  push:
    branches: [placerda-marketplace-validation-045981b4]
  workflow_dispatch:
permissions:
  contents: read
  id-token: write
jobs:
  check:
    uses: ./.github/workflows/marketplace-preflight.yml
    with:
      environment: marketplace-validation
      mode: check
