# Hosted Cockpit dependency manifest template.
#
# `__AGENTOPS_VERSION__` is substituted with the exact agentops-accelerator
# release version before deployment so the hosted App Service worker
# installs the same version validated during release (no floating/latest
# installs). No secrets, tokens, or credentials belong in this file — it is
# packaged and deployed as plain, non-secret text.
agentops-accelerator[cockpit]==__AGENTOPS_VERSION__
gunicorn==23.0.0
